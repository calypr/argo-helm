namespaces:
  argo: argo-workflows
  argocd: argocd
  tenant: wf-poc
  security: security
  argo-events: argo-events

# Argo Workflows dependency values
argo-workflows:
  server:
    enabled: true
    secure: false
    namespaced: true
    extraArgs:
      - --auth-mode=server
  controller:
    workflowNamespaces:
      - wf-poc
    # Ensure controller uses correct namespace
    namespaceInstallMode: true
  # Force namespace for all Argo Workflows components
  namespaceOverride: argo-workflows

# Argo CD dependency values
argo-cd:
  applications:
    enabled: true
    namespace: argocd                       # where Argo CD runs
  namespaceOverride: argocd
  configs:
    secret:
      create: true
      # provide a strong random key (hex or base64 is fine)
      extra: {}  # MUST be a map, not a string/boolean
      # Never commit your real token in a Git repo. instead, override it at deploy time using:
      # export ARGO_CD_SECRET=replace with output of: openssl rand -hex 32
      # helm upgrade --install ... argo-cd.configs.secret.extra."server\.secretkey"="${ARGOCD_SECRET_KEY}"
      # server.secretkey: "<?= replace with output of: openssl rand -hex 32 ?>"

# Our adapter settings
authzAdapter:
  image: ghcr.io/calypr/argo-helm:latest
  fenceBase: "https://calypr-dev.ohsu.edu/user"
  replicas: 2
  # Ensure adapter uses security namespace
  namespace: security

# On-prem S3 artifacts for Argo Workflows
s3:
  enabled: false
  hostname: ""
  bucket: ""
  region: "us-west-2"
  insecure: false
  pathStyle: true
  accessKey: ""
  secretKey: ""

# Ingresses we ship (toggle/hosts)
ingress:
  argoWorkflows:
    enabled: true
    host: "argo.localtest.me"
    tls:
      enabled: false
      secretName: ""
  argocd:
    enabled: true
    host: "argocd.localtest.me"
    tls:
      enabled: false
      secretName: ""

# NGINX auth to the adapter
ingressAuth:
  enabled: true
  authURL: "http://authz-adapter.security.svc.cluster.local:8080/check"
  passAuthorization: true

# ============================================================================
# Argo CD Applications - Multi-Application Support (RECOMMENDED)
# ============================================================================
# Use this array to deploy multiple Argo CD Applications.
# Each entry creates a separate Application resource.
#
# Example:
# applications:
#   - name: nextflow-hello-project
#     project: default
#     repoURL: https://github.com/bwalsh/nextflow-hello-project.git
#     targetRevision: main
#     path: "."
#     destination:
#       server: https://kubernetes.default.svc
#       namespace: argo
#     syncPolicy:
#       automated:
#         prune: true
#         selfHeal: true
#
#   - name: nextflow-hello-project-2
#     project: default
#     repoURL: https://github.com/bwalsh/nextflow-hello-project-2.git
#     targetRevision: main
#     path: "."
#     destination:
#       namespace: argo
#     syncPolicy:
#       automated:
#         prune: true
#         selfHeal: true
#
applications:
  - name: nextflow-hello
    repoURL: https://github.com/bwalsh/nextflow-hello-project.git
    targetRevision: main
    path: "."
    destination:
      namespace: argo
    syncPolicy:
      automated:
        prune: true
        selfHeal: true    
  - name: nextflow-hello-2
    repoURL: https://github.com/bwalsh/nextflow-hello-project-2.git
    targetRevision: main
    path: "."
    destination:
      namespace: argo
    syncPolicy:
      automated:
        prune: true
        selfHeal: true 

# ============================================================================
# DEPRECATED: Single Application Support (Use 'applications' array instead)
# ============================================================================
# This single-application configuration is deprecated and will be removed
# in a future release. Please migrate to the 'applications' array above.
#
# Migration Example:
# OLD:
#   argocdApplication:
#     enabled: true
#     name: nextflow-hello
#     repoURL: https://github.com/bwalsh/nextflow-hello-project.git
#     targetRevision: main
#     path: "."
#
# NEW:
#   applications:
#     - name: nextflow-hello
#       repoURL: https://github.com/bwalsh/nextflow-hello-project.git
#       targetRevision: main
#       path: "."
#
#argocdApplication:
#  enabled: true
#  name: nextflow-hello
#  project: default
#  repoURL: "https://github.com/bwalsh/nextflow-hello-project.git"
#  targetRevision: "main"
#  path: "."
#  # IMPORTANT: do not template here. Either leave empty or set a literal default.
##   destinationNamespace: ""   # leave empty and let the template default to .Values.namespaces.tenant


events:
  enabled: true
  namespace: argo-events

  eventBus:
    create: true
    name: default
    natsNative: {}  # use NATS native by default

  github:
    enabled: true
    name: github
    
    # ========================================================================
    # Multi-Repository Support (RECOMMENDED)
    # ========================================================================
    # Define multiple repositories in an array. Each repository gets its own
    # event name within the same EventSource.
    #
    # Example:
    repositories:
      - eventName: repo-push  # Must align with sensor event
        owner: bwalsh
        repository: nextflow-hello-project
        events: ["push"]
        active: true
      - eventName: repo-push
        owner: bwalsh
        repository: nextflow-hello-project-2
        events: ["push"]
        active: true
    
    
    # ========================================================================
    # DEPRECATED: Single Repository Configuration (backward compatibility)
    # ========================================================================
    # If 'repositories' is empty, falls back to single repository mode.
    # Use 'repositories' array above for multiple repositories.
    #
    # eventName: repo-push
    # owner: bwalsh           # <-- change as needed
    # repository: nextflow-hello-project   # <-- change as needed
    # events: ["push"]
    # active: true

    # --- GitHub Secret configuration ---
    # Secret holding either a GitHub PAT (for webhook management)
    # or a webhook secret. If tokenValue is empty, no Secret will be created.
    secret:
      create: true                       # chart will render a Secret manifest
      name: github-secret                # name of the Secret object
      tokenKey: token                    # key inside the Secret
      # Never commit your real token in a Git repo. instead, override it at deploy time using:
      # export GITHUB_PAT=github_pat_XXXXXXXX
      # helm upgrade --install --set-string events.github.secret.tokenValue=$GITHUB_PAT


    # Webhook service exposed by EventSource
    webhook:
      endpoint: /events
      port: 12000
      # IMPORTANT: Set the webhook URL that GitHub will call when events occur.
      # This must be publicly accessible from GitHub. Example:
      #   --set-string events.github.webhook.url=http://your-domain.com:12000
      # Or use the ingress configuration below with a proper hostname.
      # url: ""  # Set this via --set-string at deployment time
      service:
        type: ClusterIP
      ingress:
        enabled: true
        className: nginx
        hosts: []  # Add your public hostname, e.g., ["webhooks.example.com"]
        tls: []

  sensor:
    enabled: true
    name: run-nextflow-on-push
    workflowNamespace: wf-poc
    workflowTemplateRef: nextflow-hello-template
    parameters:
      - name: git_revision
        valueFrom: "{{(events.push.body.head_commit.id)}}"  # from GitHub payload

workflowTemplates:
  createExample: true
  namespace: wf-poc
  nextflowHello:
    name: nextflow-hello-template
    image: alpine:3.20
    command: ["/bin/sh", "-c"]
    args:
      - echo "Hello from Argo Events!"
